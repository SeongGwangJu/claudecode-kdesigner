---
description: 작업 되돌리기 — git revert 기반 비파괴 (Phase 6 작성 전, 동작 X)
model: inherit
---

# 되돌리기

> ⚠️ **Phase 6 작성 전 placeholder. 어떤 동작도 수행하지 마세요.**
>
> 이 명령은 아직 미완성이므로:
> - `git` 명령을 *어떤 형태로도 실행하지 않습니다* (`revert`·`reset`·`checkout` 모두)
> - 파일을 *생성·수정·삭제하지 않습니다*
> - `AskUserQuestion`을 *호출하지 않습니다*
>
> 사용자에게 다음 한국어 메시지만 그대로 출력하고 종료하세요:
>
> > "되돌리기 명령(`/kd:되돌리기`)은 아직 준비 중이에요. 다음 버전에서 만나요."
