# 케이디자이너 (Claude Code For KDesigner)

> 한국 디자이너가 *git·터미널 지식 없이* Claude Code로 화면을 만들고 개발자에게 깔끔히 넘기는 플러그인. 자연어로 말씀만 하시면 됩니다.

---

## 이게 뭐죠?

지금까진 보통 이런 식이었죠.

> 디자이너가 *피그마(`Figma`)*에서 화면을 그려요 → 퍼블리셔·개발자가 그걸 *진짜 화면*으로 옮겨요 → 그제서야 만져볼 수 있어요.

중간에 끼어드는 사람이 많고, 디자이너의 *원래 그림*이 옮겨지는 동안 미묘하게 비틀어지기도 합니다. 그리고 *진짜로 클릭해보기*까지 며칠이 걸려요.

**케이디자이너는 그 사이를 지웁니다.** 디자이너가 *키보드 딸깍* 한 줄로 — "이런 카드 3개 가로로 깔아줘" — 진짜로 클릭되고 스크롤되는 화면을 *바로* 만들 수 있어요. 그 결과물은 그대로 개발자에게 넘어가서 *데이터만 진짜 걸면 끝*인 상태가 됩니다.

원래 클로드 코드(`Claude Code`)는 *개발자 도구*예요. 케이디자이너는 그걸 **디자이너 도구로 탈바꿈시키는 플러그인**입니다. 영어 에러·터미널 명령·git 같은 걸 다 가리고, 한국어로만 대화하게 만들어요.

---

## 설치하면 뭐가 좋은데요?

- 🖱️ **만질 수 있는 결과물** — 피그마 시안만이 아니라 *진짜로 스크롤·클릭되는 화면*. 모바일 크기로 줄여보고 다크모드도 켜볼 수 있어요
- 🚀 **0에서 1까지 30분** — 빈 폴더에서 시작해도 30분 안에 화면 1개 + 개발자 인계까지 가능. 회사 프로젝트가 이미 있으면 그 디자인 시스템을 그대로 *이어 받아서* 새 화면을 만들어요
- 🛡️ **실수 방어** — 색·간격 한 곳 바꾸면 *전 화면을 덮치는* 사고를 자동으로 잡아주고 영향 범위를 보여줘요. 영어 에러는 자동 회복부터 시도, 안 되면 한국어로 풀어 설명
- 📦 **개발자에게 매끄럽게** — 디자이너가 작업한 그 시점 그대로 `HANDOFF.md` 한 파일에 정리. 개발자는 *데이터만 연결*하면 끝
- 🇰🇷 **그냥 한국어** — "저장해줘", "보여줘", "더 둥글게" 같은 말 한 줄이면 됩니다. git이나 npm 같은 단어를 알 필요 없어요

설치 한 번이면 매일의 디자인 사이클 — 새 화면 만들기, 점검, 저장, 인계 — 이 *그저 한국어로* 흘러갑니다.

---

## 처음 쓰시는 분께

### 1단계 — 준비물 두 가지

이 플러그인은 *클로드 코드*라는 도구 위에서, 그리고 *git*이라는 코드 가져오기 도구를 빌려 동작합니다. 둘 다 컴퓨터에 미리 깔려 있어야 해요.

#### (1) git 설치 — 안 되어 있으면

터미널(맥) 또는 PowerShell(윈도우)에 다음을 쳐서 *버전 숫자가 보이면 통과*입니다:

```bash
git --version
```

*command not found* 같은 메시지가 뜨면 아래 본인 운영체제 안내를 펼쳐서 따라 해주세요.

<details>
<summary><b>🪟 윈도우에서 git 설치</b></summary>

1. 구글에서 **`git windows`** 검색 → 첫 결과(*Git for Windows*)에 들어가 설치 파일을 받아 실행
2. 설치 마법사가 묻는 옵션은 *대부분 그냥 Next* 눌러도 됩니다
3. 중간에 **기본 브랜치 이름**(`Initial branch name`)을 묻는 화면이 있어요. 옛날엔 `master`였는데 요즘은 **`main`** 으로 짓는 게 표준이에요. 직접 입력 칸에 **`main`** 이라고 적고 진행
4. 설치 끝난 뒤 PowerShell을 *새로 한 번 닫았다 열고* `git --version` 다시 쳐보면 버전이 나와요

</details>

<details>
<summary><b>🍎 맥에서 git 설치 (Homebrew 경유)</b></summary>

1. *런치패드*(Launchpad)에서 **`터미널`** 검색해서 열기
2. https://brew.sh/ 에 들어가면 화면 가운데에 *긴 설치 명령어 한 줄*이 보여요. 그 한 줄을 *그대로 복사*해서 터미널에 붙여 넣고 ↵Enter
   - *Homebrew*는 맥용 *프로그램 설치 도우미*예요. 한 번 깔아두면 다른 도구 깔 때도 계속 씁니다
   - 설치는 *조금 오래 걸려요* (몇 분). 화면에 글이 막 흘러도 정상이니 기다려주세요
   - 중간에 *Password:* 라고 비밀번호를 물으면 **맥북 로그인 비밀번호**를 입력하세요 (입력해도 화면에 안 보이는데 정상이에요)
3. 설치가 끝나면 화면 끝에 *Run these two commands...* 라며 **명령어 2줄**이 보여요. 그 2줄을 *각각 한 줄씩* 복사해서 터미널에 붙여 넣고 ↵Enter (제 거 따라 적지 말고 *화면에 본인 컴퓨터에 맞게 표시된 그대로* 붙여 넣어야 해요)
4. 마지막으로 git 설치:

   ```bash
   brew install git
   ```

5. `git --version` 쳤을 때 버전이 나오면 끝

</details>

> *git 설치 가이드 출처: [코딩애플 — git 설치하기 (Windows·Mac)](https://codingapple.com/unit/git-install-windows-mac/)*

#### (2) 클로드 코드 설치

👉 **[클로드 코드 한국어 설치 가이드 (공식)](https://docs.claude.com/ko/docs/claude-code/setup)**

가이드 안의 설치 명령 한 줄을 *맥의 터미널 앱* 또는 *윈도우의 PowerShell*에서 실행하시면 됩니다. 처음 한 번 *로그인*까지 끝내두면 다음부턴 자동이에요.

> ⚠️ **데스크탑 앱(`Claude Desktop App`)을 쓰고 계신가요?**
> 데스크탑 앱 안의 클로드 코드 패널에선 *플러그인 설치 명령(`/plugin`)이 아직 작동하지 않아요* (`"/plugin isn't available in this environment"`). **이 2단계 설치만큼은 터미널을 한 번 열어** 진행해주세요. 설치가 끝나면 평소처럼 데스크탑 앱에서도 쓰실 수 있습니다.

### 2단계 — 이 플러그인 설치

먼저 *클로드 코드를 켜야* 해요.

- **터미널 CLI** — *맥 터미널 앱* 또는 *윈도우 PowerShell*을 열고 `claude`라고 치면 켜집니다. 켜진 상태에서 아래 두 줄을 입력하세요.
- **데스크탑 앱(이 단계만 해당 X)** — 위 ⚠️ 안내대로 **이 설치 단계는 터미널에서**만 가능합니다. 설치 후엔 데스크탑 앱에서 *어디서든* 쓸 수 있어요.

켜진 화면에서 다음 두 줄을 차례로 입력하세요. (앞에 `/` 그대로 붙여서)

```
/plugin marketplace add https://github.com/SeongGwangJu/claudecode-kdesigner.git
/plugin install kdesigner@claudecode-kdesigner
```

첫 줄이 *플러그인 카탈로그를 등록*, 둘째 줄이 *실제 설치*입니다.

#### 설치 중간에 이런 화면이 뜨면

```
> Install for you (user scope)
  Install for all collaborators on this repository (project scope)
  Install for you, in this repo only (local scope)
```

→ **첫 번째 항목 (`Install for you`, user scope) 권장**. *내 컴퓨터 어느 폴더에서든* 디자이너 모드로 작동합니다. (회사 동료와 함께 쓸 거면 두 번째, 이 한 폴더에서만 쓸 거면 세 번째)

#### 설치 중간에 이런 경고가 뜨면

```
⚠ Make sure you trust a plugin before installing...
Will install:
· Component summary not available for remote plugin
```

→ 정상입니다. *원격에서 가져오는 플러그인은 미리보기가 안 뜨는 게 기본*이에요. 그대로 진행하세요.

#### 설치 완료 확인

설치가 성공하면 이런 메시지가 뜹니다:

```
✓ Installed kdesigner. Run '/reload-plugins' to apply.
```

→ 메시지 그대로 **`/reload-plugins`** 한 줄을 그 다음에 입력해주세요. 그래야 방금 깐 플러그인이 *바로 이 자리에서* 활성화돼요. (안 치면 클로드 코드를 한 번 껐다 켜야 잡힙니다)

`/reload-plugins`까지 끝내면 명령어 자동완성에 `/kdesigner:`로 시작하는 것들이 잡히기 시작해요.

### 3단계 — 작업 폴더로 가서 시작 의식 한 번

```
/kdesigner:디자인초기설정
```

`/`만 친 다음 *초기*까지만 쳐도 자동완성에 *디자인초기설정*이 잡혀요 — 그대로 ↵Enter. "이 컴퓨터 전체에서 켤까요? / 이 프로젝트만 켤까요?" 묻습니다 — *잘 모르겠으면 이 프로젝트만*이 안전합니다.

### 4단계 — 자연어로 시작

> "새로 만들고 싶어" 또는 "기존 프로젝트에서 가져오기"

응답 끝에 따라오는 *다음 행동 제안*만 따라가시면 30분 안에 화면 1개 + 개발자 인계까지 됩니다.

---

## 이렇게 말하면 동작해요

자연어 한 줄이면 알아서 흘러갑니다. 슬래시는 비상용이니 평소엔 무시하세요.

| 이렇게 말씀하시면 | 무슨 일이 일어나요 |
|---|---|
| "새로 만들고 싶어" / "처음부터 만들어줘" | 빈 폴더에서 새 프로젝트를 셋업하고 첫 화면 만들 준비까지 끝냅니다 |
| "기존 프로젝트에서 가져오기" | 회사 코드에 안전하게 끼어들어 컴포넌트를 정리해드려요 (기존 파일은 손대지 않아요) |
| "보여줘" / "한번 띄워봐" / "화면 켜줘" | 의존성 설치·서버 시작·브라우저 열기까지 한 번에 |
| "저장해줘" / "임시저장" / "올려줘" | 지금까지 작업을 한 시점으로 묶어둡니다 (나중에 그 시점으로 되돌릴 수 있어요) |
| "점검해줘" / "확인해줘" | 모바일 잘림·색 대비·이미지 설명 누락·디자인 토큰 어긋남을 사람 말로 짚어드려요 |
| "개발자한테 넘기기" / "인계 정리해줘" | 개발자가 1분 안에 알아볼 수 있게 `HANDOFF.md`로 정리합니다 |
| "이 버튼 좀 더 둥글게" / "색 부드럽게" | 이 화면만 바꿀지, 전체에 적용할지 자동으로 묻고 일관성을 지켜드려요 |
| "에러 났어" / "왜 안 돼?" | 영어 에러는 자동 회복부터 시도, 안 되면 친화 한국어 + 개발자에게 보낼 메시지를 따로 만들어드려요 |

---

## 만들어지는 폴더 약속

새로 만들거나 가져온 프로젝트엔 다음 폴더가 자동으로 생깁니다.

- `asset/` — 이미지·아이콘·폰트
- `mock/` — 가짜 데이터 (개발자가 실제 데이터로 교체할 자리)
- `components/` — 재사용 컴포넌트

자연어 "이 이미지 추가해줘"는 자동으로 `asset/`에 들어가고 컴포넌트에서 바로 참조됩니다.

---

## 자동으로 막아주는 위험

디자이너가 의식하지 않아도 뒤에서 가드가 돕습니다.

- **에러 자동 회복** — *필요한 도구 누락*(영어로 `dependency missing`)·*화면 출력 통로 충돌*(영어로 `port in use`)·일시적 네트워크 오류는 자동으로 다시 시도합니다. 영어 에러를 그대로 던지지 않아요
- **저장 직전 검증** — 큰 작업 마무리 시 자동으로 한 번 점검을 돌립니다. 텍스트 사소한 변경엔 발동 X (시간 낭비 없이)
- **디자인 토큰 일관성** — 한 화면에서 시작한 색·간격 변경이 *전 화면을 덮치는* 사고를 막아드려요. 영향 범위(예: "이 색은 다른 3곳에서도 쓰여요")를 보여주고 묻습니다

---

## 개발자에게 넘길 때

"개발자한테 넘기기"라고 말씀하시면 `HANDOFF.md`가 자동으로 생깁니다. 그 한 파일에:

- **그대로 가져갈 수 있는 부분** (화면·컴포넌트·디자인 토큰)
- **실제 데이터에 연결해야 할 곳** (파일·줄 번호 단위로)
- **가짜 데이터 위치**와 실제 API가 가져야 할 형태 힌트
- **인계 시점의 저장 표식** (`commit 해시`) — 개발자가 정확히 그 시점을 다시 볼 수 있게

개발자분께 이 한 파일만 보여드리면 됩니다.

---

## 비상용 슬래시 커맨드 ⚠️

자연어가 발동되지 않거나 위험 동작을 명시적으로 트리거하고 싶을 때만 씁니다. 평소에는 무시하세요.

- `/kdesigner:디자인초기설정` — 디자이너 모드 시작 (1회성). 전역으로 켤지 묻습니다
- `/kdesigner:디자인끄기` — 디자이너 모드 종료. 전역 설정도 *원본 그대로* 복원
- `/kdesigner:되돌리기` — 작업 되돌리기. *되돌리기*(`git revert`) 기반이라 되돌린 것도 다시 되돌릴 수 있어요 (안전)

> 💡 **팁**: `/`만 친 다음 뒷부분(예: *초기*, *끄기*, *되돌*)만 쳐도 자동완성이 잡아줍니다. 굳이 `/kdesigner:`를 다 칠 필요 없어요.

---

## 잘 안 될 때

- **자연어로 말했는데 발동 안 함** — 같은 동작을 슬래시로도 부를 수 있어요. "보여줘"가 안 먹으면 `/kdesigner:디자인초기설정`이 켜져 있는지 먼저 확인하세요 (켜져 있다면 표현을 살짝 바꿔서 다시)
- **영어 에러가 그대로 보임** — 자동 회복이 실패한 경우입니다. 화면에 같이 뜨는 *개발자에게 복사해 보내주세요* 블록을 그대로 개발자에게 전달해주세요. 본문은 한국어로 정리되어 있어요
- **명령어가 안 보여요** — 클로드 코드 *재시작* 한 번 해보세요. 설치 직후엔 새 명령어가 잡히는 데 시간이 좀 걸리기도 합니다

---

## 개발자분께 (혹시 이 글을 읽고 계시다면)

디자이너에게 인계할 환경에 다음 파일들이 함께 들어가야 매끄럽습니다.

- `package.json` + 잠금 파일 (`pnpm-lock.yaml` / `package-lock.json` / `yarn.lock` 중 하나)
- `tailwind.config.*`, `postcss.config.*`, `tsconfig.json`
- `app/globals.css` 또는 색·토큰 정의 파일
- 핵심 라우트 1개 또는 *디자인 시스템 페이지* — 컴포넌트 사용 예시 참조용

자세한 인계 권장 흐름은 `kdesigner:디자인초기설정`이 만드는 `CLAUDE.project.md` 안의 *인계 주의사항* 섹션을 참고하세요.

---

이 플러그인은 한국 디자이너가 매일 도는 사이클이 *그저 한국어로* 흘러가게 만드는 게 목표예요. 막히는 부분이 있다면 [GitHub Issues](https://github.com/SeongGwangJu/claudecode-kdesigner/issues)로 알려주세요.
